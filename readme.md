# gmod-unstuck

This is a small system that monitors player movement to detect (and solve) players that are stuck.

This requires maps to have navigation data available (the same data used by NPCs and NextBots).
